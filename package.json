{
  "name": "moshify",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "targets": {
    "default": {
      "distDir": "./dist",
      "sourceMap": false,
      "engines": {
        "browsers": "> 0.25%, not dead"
      }
    }
  }
}
